# Model configurations

Put project-specific YOLO architecture YAML files here. The VisDrone dataset YAML is generated by `src/prepare_visdrone.py` inside the ignored `data/VisDrone/` directory so its absolute path matches each teammate's machine.

